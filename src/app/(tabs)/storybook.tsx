import { Text, View } from "react-native";

export default function Storybook() {
  return (
    <View
      style={{
        flex: 1,
        justifyContent: "center",
        alignItems: "center",
      }}
    >
      <Text>Storybook</Text>
    </View>
  );
}
